<UserControl x:Class="RPG.View.TriggerEntityUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Canvas.Left="{Binding position.X}" Canvas.Top="{Binding position.Y}" Width="{Binding img.Width}" Height="{Binding img.Height}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Image Source="{Binding img.Source}" />
        <TextBlock Text="{Binding text}" Width="{Binding img.Width}" Height="{Binding img.Height}" FontSize="15" Foreground="{Binding col}" TextAlignment="Center"/>
    </Grid>
</UserControl>
